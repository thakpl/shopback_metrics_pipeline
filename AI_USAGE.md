# AI Usage Documentation

| Prompt Goal | Example Prompt | AI Response Summary | How You Used or Modified It | What You Learned / Observed |
| --- | --- | --- | --- | --- |
| Design SQL logic for `avg_order_revenue` | "How to calculate average revenue per order by customer in DuckDB when raw data is split between orders and order_items tables?" | Provided a foundational query joining `orders` and `order_items` on `order_id` with basic aggregations. | Adopted the JOIN logic but modified the calculation to use `COUNT(DISTINCT o.order_id)` to handle the one-to-many relationship correctly. | Observed the risk of data duplication when joining transactional tables; learned to safeguard metrics against inflating counts. |
| Handle edge cases in YAML validation | "My Python script crashes with `TypeError: 'NoneType' object is not subscriptable` when iterating through the `/metrics` folder. How do I fix this?" | Explained that `yaml.safe_load()` returns a `NoneType` object when reading an entirely empty YAML file. | Added a defensive check (`if metric is None: continue`) to gracefully skip empty files and utilized `.get()` for safe dictionary fetching. | Learned that empty files evaluate to `None` in PyYAML; recognized the necessity of defensive coding for stable pipeline operations. |
| Debug DuckDB execution error | "DuckDB throws `Catalog Error: Table does not exist` when trying to query the table at the end of the script, even though the code looks correct. What went wrong?" | Pointed out that executing the script from outside the project root directory breaks the relative path resolution for the `/metrics` folder. | Fixed the execution context in the terminal and added strict, clear setup/run instructions in the `README.md` to guide users. | Observed how running execution paths heavily affect data pipelines utilizing relative paths; learned the high value of proper handover documentation. |
